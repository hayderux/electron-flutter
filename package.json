{
  "name": "electron-quick-start-flutter",
  "version": "1.0.0",
  "description": "A minimal Electron application with flutter & dart",
  "main": "web/main.js",
  "scripts": {
    "build": "npm run dev && pub run build_runner build --config=release -o build",
    "build_runner_serve": "pub run build_runner serve --hot-reload",
    "dev": "run-p \"build_runner_serve\" \"start\"",
    "start": "electron ."
  },
  "repository": "https://github.com/electron/electron-quick-start",
  "author": "GitHub",
  "license": "MIT",
  "devDependencies": {
    "electron": "^6.0.1",
    "npm-run-all": "^4.1.5"
  }
}