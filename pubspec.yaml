name: hello_world
description: An app built using Flutter for web

environment:
  sdk: '>=2.6.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  pedantic: ^1.9.0
  
flutter:
  uses-material-design: true